name: test0729

on:
  workflow_dispatch:

jobs:
  build:
    name: 构建
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: ddd  
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: sleep 10m     
